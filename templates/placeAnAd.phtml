<?php if ($flashMessage) : ?>
    <div class="fixed bottom-0 right-0 m-4 bg-green-500 text-white text-center py-3 px-4 rounded shadow-md flash-message">
    <?= htmlspecialchars($flashMessage); ?>
</div>
<?php endif; ?>



<div class="flex items-center justify-center text-justify">
    <div class="bg-color">
        <!-- <p class="text-lg bg-color font-semibold text-gray-700">Vendez votre véhicule</p> -->
    </div>
    <img src="<?= asset('/img/checklist.png') ?>" class="iconePlace" alt="logo">
    <p class="flxT">Transformez votre voiture en argent comptant avec notre plateforme de vente conviviale.</p>
</div>



     <div class="lg:flex">

        <div class="lg:w-1/3 w-full lg:order-1 order-2 p-4  rounded-md  mb-4 lg:mb-0">
            <!-- Contenu de l'image-carousel -->
            <div id="image-carousel">
        </div>
    </div> 

<div class="container mx-auto">
    <!-- Section de droite (formulaire) -->
    <div class="lg:w-3/4 w-full p-4 bg-white rounded-md shadow-md mt-4 lg:mt-0 text-center">
        <form class="space-y-4" enctype="multipart/form-data" method="POST" action="">
            <div class="flex flex-wrap -mx-2">
                <div class="w-full md:w-1/2 px-2 mb-4 md:mb-0">
                    <label for="originalFileName" class="block text-center font-semibold">Photo :</label>
                    <input type="file" id="originalFileName" name="originalFileName" accept="image/*" onchange="afficherPhoto()" required class="w-full h-12 border rounded-md p-2">
                    <?php if (isset($errors['originalFileName'])) : ?>
                        <p class="text-red-500"><?= htmlspecialchars($errors['originalFileName']); ?></p>
                    <?php endif; ?>
                </div>
                <div class="w-full md:w-1/2 px-2 mb-4 md:mb-0">
                    <label for="fuel" class="block text-center font-semibold">Carburant :</label>
                    <select id="fuel" name="fuel" required class="w-full h-12 border rounded-md p-2">
                        <option value="essence">Essence</option>
                        <option value="diesel">Diesel</option>
                        <option value="hybride">Hybride</option>
                        <option value="electrique">Électrique</option>
                    </select>
                    <?php if (isset($errors['fuel'])) : ?>
                        <p class="text-red-500"><?= htmlspecialchars($errors['fuel']); ?></p>
                    <?php endif; ?>
                </div>
                <div class="w-full md:w-1/2 px-2 mb-4 md:mb-0">
                    <label for="price" class="block text-center font-semibold">Prix :</label>
                    <input type="number" class="w-full h-12 border rounded-md p-2" id="price" name="price" required>
                    <?php if (isset($errors['price'])) : ?>
                        <p class="text-red-500"><?= htmlspecialchars($errors['price']); ?></p>
                    <?php endif; ?>
                </div>
                <div class="w-full md:w-1/2 px-2 mb-4 md:mb-0">
                    <label for="make" class="block text-center font-semibold">Marque :</label>
                    <input type="text" class="w-full h-12 border rounded-md p-2" id="make" name="make" required>
                    <?php if (isset($errors['make'])) : ?>
                        <p class="text-red-500"><?= htmlspecialchars($errors['make']); ?></p>
                    <?php endif; ?>
                </div>
                <div class="w-full md:w-1/2 px-2 mb-4 md:mb-0">
                    <label for="years" class="block text-center font-semibold">Année :</label>
                    <input type="number" class="w-full h-12 border rounded-md p-2" id="years" name="years" min="1990" max="2100" required>
                    <?php if (isset($errors['years'])) : ?>
                        <p class="text-red-500"><?= htmlspecialchars($errors['years']); ?></p>
                    <?php endif; ?>
                </div>
                <div class="w-full md:w-1/2 px-2">
                    <label for="mileage" class="block text-center font-semibold">Kilométrage :</label>
                    <input type="number" class="w-full h-12 border rounded-md p-2" id="mileage" name="mileage" required>
                    <?php if (isset($errors['mileage'])) : ?>
                        <p class="text-red-500"><?= htmlspecialchars($errors['mileage']); ?></p>
                    <?php endif; ?>
                </div>
            </div>
            <div class="text-center">
                <label for="accounte" class="block font-semibold">Description :</label>
                <textarea id="accounte" name="accounte" rows="5" cols="25" class="w-full border rounded-md p-2"></textarea>
                <?php if (isset($errors['accounte'])) : ?>
                    <p class="text-red-500"><?= htmlspecialchars($errors['accounte']); ?></p>
                <?php endif; ?>
            </div>
            <div class="text-center">
                <input type="submit" value="Envoyer" class="bg-blue-500 hover:bg-blue-600 text-white font-semibold py-3 px-4 rounded-md w-full">
            </div>
        </form>
    </div>

